import Box from '@mui/material/Box';
import { RawTitlePrincipalTable } from './RawTitlePrincipalTable.tsx';

export function RawTitlePrincipalPage() {
	return (
		<Box sx={{ flex: 1, display: 'flex' }}>
			<RawTitlePrincipalTable />
		</Box>
	);
}
